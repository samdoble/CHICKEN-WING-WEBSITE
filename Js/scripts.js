// scripts.js

// Example of a simple alert function on the homepage
document.addEventListener('DOMContentLoaded', (event) => {
    if (document.querySelector('body').classList.contains('home')) {
        alert("Welcome to Chicken Wings Paradise!");
    }
});
